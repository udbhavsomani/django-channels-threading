# django-channels-threading

## Setup
```
docker-compose up --build
```

## Usage
- Server starts at http://localhost:8000/
- Go to http://localhost:8000/chat/<room_name>/ to start chatting
    > Default login creds: admin/admin